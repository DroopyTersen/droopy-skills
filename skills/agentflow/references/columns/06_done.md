# Done

The requested delivery is complete and verified. For code changes, the explicitly authorized merge and required post-merge checks have succeeded; when deployment was requested, that delivery is verified too. Confirm local `main` was updated after a merge into `main`.

Add one short item comment with the merged PR link, what was completed and verified, the delivered environment when applicable, and local-main synchronization status. Mark acceptance criteria complete only when supported by evidence, then move to Done and verify the provider state. No separate completion document or history table.

For research or configuration work with no PR, use the agreed deliverable and its verification as the completion evidence; do not invent a PR requirement. A status-only request should be followed accurately without claiming unperformed delivery.

Follow the repository's normal remote-branch deletion setting. Keep local branches and worktrees unless Andrew requests cleanup; they may hold useful sessions or unrelated work. Do not automatically start another card.
